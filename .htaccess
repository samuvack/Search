Options -Indexes
allow from all

<IfModule mod_rewrite.c>
    Options -MultiViews


    RewriteEngine On
    RewriteCond  %{DOCUMENT_ROOT}/geosearch/web/$1 -f
    RewriteRule  ^(.*)$  /geosearch/web/$1  [L,QSA]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ bootstrap.php [QSA]
</IfModule>
